name: Evaluate AccelerometerTouchTrackerSdk

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'AccelerometerTouchTrackerSdk/**'
  push:
    branches: [ main ]
    paths:
      - 'AccelerometerTouchTrackerSdk/**'

jobs:
  evaluate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build AccelerometerTouchTrackerSdk
        run: ./gradlew :AccelerometerTouchTrackerSdk:build

      - name: Run unit tests
        run: ./gradlew :AccelerometerTouchTrackerSdk:test

      - name: Run Android Lint
        run: ./gradlew :AccelerometerTouchTrackerSdk:lint

      - name: Run ktlint
        run: ./gradlew :AccelerometerTouchTrackerSdk:ktlintCheck

      - name: Run detekt
        run: ./gradlew :AccelerometerTouchTrackerSdk:detekt

      - name: Generate Dokka documentation
        run: ./gradlew :AccelerometerTouchTrackerSdk:dokkaHtml
